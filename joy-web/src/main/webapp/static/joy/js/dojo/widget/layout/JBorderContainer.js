define([
	"dojo/_base/declare",
	"dijit/layout/BorderContainer"
	], 
	
	function(declare, BorderContainer) {
		return declare("joy.layout.JBorderContainer", BorderContainer, {
			
			
		});
	}

);